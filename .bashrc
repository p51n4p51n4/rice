#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

PS1='[\u@\h \w]\$ '

alias zed=zeditor

alias ls='ls -a --color=auto'
alias grep='grep --color=auto'
alias rm='rm -rf'
alias cp='cp -r'
